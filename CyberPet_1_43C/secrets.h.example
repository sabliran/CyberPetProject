// Copy this file to secrets.h (same directory) and fill in your WiFi.
// secrets.h is gitignored so credentials never reach the repository.
#pragma once
#define WIFI_SSID_SECRET     "your-wifi-name"
#define WIFI_PASSWORD_SECRET "your-wifi-password"
